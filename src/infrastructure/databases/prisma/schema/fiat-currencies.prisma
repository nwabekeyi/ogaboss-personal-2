model FiatCurrency {
  id        String   @id @default(cuid())
  code      String   @unique
  name      String
  symbol    String?
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  usdRate   String   @default("0")

  @@map("fiatcurrencies")
}
