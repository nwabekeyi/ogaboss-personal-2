export enum RefundStatus {
    PENDING = 'pending',
    PROCESSING = 'processing',
    AWAITING_APPROVAL = 'awaiting_approval',
    REFUNDED = 'refunded',
    COMPLETED = 'completed',
    FAILED = 'failed',
    CANCELLED = 'cancelled',
    REJECTED = 'rejected',
  }